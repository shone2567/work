# This file describes the network interfaces available on your system
# and how to activate them. For more information, see interfaces(5), ifup(8)
#
# Please see /usr/share/doc/python-ifupdown2/examples/ for examples
#
#

# The loopback network interface
auto lo
iface lo inet loopback

# The primary network interface
auto eth0
iface eth0 
	address 172.31.6.5/16
	gateway 172.31.0.1

%for port in range(1, 52):
	auto swp${port}
	iface swp${port}
%endfor

auto bridge
iface bridge
	bridge-vlan-aware yes
	bridge-ports glob swp1-52
	bridge-vids 100-105 1000-1030
	bridge-pvid 1
	bridge-stp on
